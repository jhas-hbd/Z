<div class="search_form">
  <%= form_with url: search_books_path, local: true, method: :get do |f| %>
    <%= f.select :days, Book.stays.keys, {} %>
    <%= f.select :price, Book.costs.keys, {} %>
    <%= f.submit "検索" %>
  <% end %>
</div>